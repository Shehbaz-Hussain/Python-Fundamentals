"""
===============================================================================
File Name: exercise08.py
Exercise Title: Calculate the Power of a Number
Topic: Lambda Functions with Parameters and Return Values
Description:
    Practice creating a lambda function that accepts a base and an exponent,
    then returns the calculated power.

Concepts Covered:
    - Lambda functions
    - Multiple parameters
    - Return values
    - Exponentiation operator
    - Function invocation

Python Version:
    Python 3.13+
===============================================================================

Learning Objective
------------------
Learn how to create a lambda function that performs exponentiation using two
parameters and returns the computed result.

Problem Statement
-----------------
Create a lambda function that accepts two numbers:
- the base
- the exponent

The function should return the value of the base raised to the given exponent.

Store the lambda function in a meaningful variable and test it with different
base and exponent values.

Requirements
------------
1. Create a lambda function that accepts two parameters.
2. Use the exponentiation operator (`**`) to calculate the result.
3. Return the calculated value.
4. Store the lambda function in a meaningful variable.
5. Call the function with at least four different pairs of values.
6. Display each result using the print() function.

Constraints
-----------
- Use a lambda function.
- Do not define a regular function using the `def` keyword.
- The lambda expression must consist of a single expression.
- Use meaningful variable names.

Hints
-----
- Python uses the `**` operator for exponentiation.
- For example:

    2 ** 3

  evaluates to:

    8

- Test the function with different exponents, including 0 and 1.

Expected Output
---------------
Your output should be similar to the following:

8
25
1
81

(The exact values depend on the inputs you choose.)

Related Concepts
----------------
- Lambda Functions
- Function Parameters
- Return Values
- Exponentiation Operator
- Arithmetic Expressions
- Function Calls
"""