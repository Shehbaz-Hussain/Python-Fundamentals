"""
===============================================================================
File Name: exercise04.py
Exercise Title: Determine Whether a Number Is Even
Topic: Basic Lambda Function Syntax
Description:
    Practice creating a lambda function that evaluates a condition and returns
    a Boolean value indicating whether a number is even.

Concepts Covered:
    - Lambda functions
    - Anonymous functions
    - Function parameters
    - Boolean return values
    - Modulus operator
    - Function invocation

Python Version:
    Python 3.13+
===============================================================================

Learning Objective
------------------
Learn how to write a lambda function that performs a simple logical test and
returns either True or False.

Problem Statement
-----------------
Create a lambda function that accepts one integer and returns True if the
number is even; otherwise, return False.

Store the lambda function in a meaningful variable and test it with several
different numbers.

Requirements
------------
1. Create a lambda function that accepts one parameter.
2. Use the modulus (%) operator to determine whether the number is even.
3. Return a Boolean value (True or False).
4. Store the lambda function in a meaningful variable.
5. Call the function with at least five different integers.
6. Display each result using the print() function.

Constraints
-----------
- Use a lambda function.
- Do not define a regular function using the 'def' keyword.
- Do not use loops or conditional statements outside the lambda unless needed
  for testing.
- Keep the lambda expression simple and readable.

Hints
-----
- A number is even if it is divisible by 2 without a remainder.
- The modulus operator (%) returns the remainder after division.
- Compare the remainder with 0.

Expected Output
---------------
Your output should be similar to the following:

True
False
True
False
True

(The exact results depend on the numbers you choose.)

Related Concepts
----------------
- Lambda Functions
- Anonymous Functions
- Boolean Values
- Modulus Operator
- Comparison Operators
- Function Parameters
- Return Values
"""