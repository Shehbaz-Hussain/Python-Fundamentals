# Exercise 03
# Check whether a product price is less than or equal to the budget.

price = float(input("Enter the product price: "))
budget = float(input("Enter your budget: "))

print(price <= budget)