# Exercise 04
# Check whether an employee's salary is greater than or equal to the required salary.

salary = float(input("Enter the employee's salary: "))
required_salary = float(input("Enter the required salary: "))

print(salary >= required_salary)