# Conditional Statements — Quiz

## Instructions

- Answer all questions without referring to notes.
- Choose the most appropriate answer for each multiple-choice question.
- Write code for the programming questions.
- Review your answers after completing the quiz.

---

# Part A — Multiple Choice Questions (MCQs)

### 1. Which keyword is used to make a decision in Python?

A. `loop`  
B. `switch`  
C. `if`  
D. `case`

---

### 2. Which statement is executed when an `if` condition evaluates to `False`?

A. `elif` always executes  
B. `else`, if it exists  
C. The program stops  
D. The first statement in the file

---

### 3. Which operator checks whether two values are equal?

A. `=`  
B. `==`  
C. `!=`  
D. `>=`

---

### 4. What is the output?

```python
number = 10

if number > 5:
    print("A")
else:
    print("B")
```

A. `B`  
B. `10`  
C. `A`  
D. Error

---

### 5. Which statement is used to check multiple conditions?

A. `repeat`
B. `elif`
C. `continue`
D. `pass`

---

### 6. Which logical operator returns `True` only when both conditions are `True`?

A. `or`
B. `not`
C. `and`
D. `==`

---

### 7. Which symbol is required at the end of every `if`, `elif`, and `else` statement?

A. `;`
B. `,`
C. `:`
D. `.`

---

### 8. What is the output?

```python
age = 16

if age >= 18:
    print("Adult")
else:
    print("Minor")
```

A. Adult  
B. Minor  
C. 16  
D. Error

---

### 9. What is a nested `if` statement?

A. An `if` inside another `if` or `else`
B. Two unrelated `if` statements
C. An `if` without an `else`
D. A loop inside an `if`

---

### 10. Which statement about indentation in Python is correct?

A. It is optional.
B. It is used only for comments.
C. It defines code blocks.
D. It is ignored by Python.

---

# Part B — Short Answer Questions

1. What is a conditional statement?

2. What is the difference between an `if` statement and an `if-else` statement?

3. Why is the `elif` statement useful?

4. What is the purpose of comparison operators?

5. What is the purpose of logical operators?

6. Explain the difference between `=` and `==`.

7. What is a nested `if` statement?

8. Why is indentation important in Python?

9. Can an `if` statement exist without an `else` statement? Explain.

10. What happens if none of the conditions in an `if-elif-else` statement evaluate to `True` and there is no `else` block?

---

# Part C — Predict the Output

### 1.

```python
x = 8

if x > 5:
    print("Yes")
else:
    print("No")
```

Output:

```
____________________
```

---

### 2.

```python
number = 3

if number % 2 == 0:
    print("Even")
else:
    print("Odd")
```

Output:

```
____________________
```

---

### 3.

```python
marks = 72

if marks >= 90:
    print("A")
elif marks >= 80:
    print("B")
elif marks >= 70:
    print("C")
else:
    print("F")
```

Output:

```
____________________
```

---

### 4.

```python
age = 20

if age >= 18:
    if age >= 60:
        print("Senior")
    else:
        print("Adult")
```

Output:

```
____________________
```

---

### 5.

```python
value = -5

if value > 0:
    print("Positive")
elif value < 0:
    print("Negative")
else:
    print("Zero")
```

Output:

```
____________________
```

---

# Part D — Programming Questions

### 1.

Write a program that checks whether a number is positive, negative, or zero.

---

### 2.

Write a program that determines whether a person is eligible to vote.

---

### 3.

Write a program that accepts marks and prints the corresponding grade using an `if-elif-else` statement.

---

### 4.

Write a program that accepts three numbers and prints the largest number.

---

### 5.

Write a program that asks the user to enter a username and password and displays a success message only if both values match predefined credentials.

---

# Self-Evaluation Checklist

- I can write an `if` statement.
- I can write an `if-else` statement.
- I can write an `if-elif-else` statement.
- I can write a nested `if` statement.
- I can use comparison operators correctly.
- I can combine conditions using logical operators.
- I understand the importance of indentation.
- I can trace the execution of conditional statements.