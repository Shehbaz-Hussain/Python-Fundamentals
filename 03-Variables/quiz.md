# quiz.md

# Python Variables Quiz

This quiz tests your understanding of variables in Python.

---

## Multiple Choice Questions (50)

1. What is a variable in Python?
   - A. A reserved keyword
   - B. A named container for data
   - C. A function
   - D. A module

2. Which symbol is used for assignment?
   - A. `==`
   - B. `=`
   - C. `!=`
   - D. `+`

3. Which of the following is a valid variable name?
   - A. `2name`
   - B. `user-name`
   - C. `user_name`
   - D. `class`

4. What does `print(name)` do?
   - A. Deletes the variable
   - B. Displays the value of `name`
   - C. Creates a new variable
   - D. Reassigns the variable

5. Which of these is a Python keyword?
   - A. `name`
   - B. `value`
   - C. `if`
   - D. `score`

6. What is the result of `x = 10` followed by `x = 20`?
   - A. Error
   - B. `x` becomes `20`
   - C. `x` remains `10`
   - D. Nothing happens

7. Which of the following is a string?
   - A. `10`
   - B. `True`
   - C. `"hello"`
   - D. `3.14`

8. What is the purpose of `type()`?
   - A. Deletes a variable
   - B. Checks the data type
   - C. Assigns a value
   - D. Swaps values

9. What is the purpose of `id()`?
   - A. Returns a variable name
   - B. Returns the memory identity of an object
   - C. Prints a string
   - D. Converts to integer

10. Which naming style is recommended by PEP 8?
    - A. `CamelCase`
    - B. `snake_case`
    - C. `UPPERCASE`
    - D. `kebab-case`

11. What does the line `a, b = 1, 2` do?
    - A. Creates one variable
    - B. Assigns two values to two variables
    - C. Causes an error
    - D. Swaps values

12. Which of these is mutable?
    - A. `int`
    - B. `str`
    - C. `list`
    - D. `tuple`

13. What is the output of `print(3 + 4)`?
    - A. `34`
    - B. `7`
    - C. `3 + 4`
    - D. `Error`

14. Which of the following is an invalid variable name?
    - A. `user_name`
    - B. `name2`
    - C. `user-name`
    - D. `name`

15. What does `del name` do?
    - A. Creates `name`
    - B. Deletes `name`
    - C. Prints `name`
    - D. Renames `name`

16. Which one is a boolean value?
    - A. `"True"`
    - B. `False`
    - C. `0`
    - D. `None`

17. What does the assignment `x = y = 5` do?
    - A. Creates separate values
    - B. Assigns the same value to both variables
    - C. Swaps the values
    - D. Raises an error

18. Which of the following is a float?
    - A. `5`
    - B. `True`
    - C. `5.0`
    - D. `"5"`

19. What is the difference between `=` and `==`?
    - A. Both compare values
    - B. Both assign values
    - C. `=` assigns, `==` compares
    - D. `==` assigns, `=` compares

20. Which code is correct for swapping two variables?
    - A. `a = b`
    - B. `a, b = b, a`
    - C. `swap(a, b)`
    - D. `a == b`

21. What is the output of `x = 10; print(x)`?
    - A. `10`
    - B. `x`
    - C. `Error`
    - D. `None`

22. Which of these is a good variable name?
    - A. `x`
    - B. `a`
    - C. `student_name`
    - D. `temp1234`

23. What happens if you use a Python keyword as a variable name?
    - A. It works normally
    - B. It causes an error
    - C. It creates a new keyword
    - D. It becomes a comment

24. What does `name = "Alice"` mean?
    - A. Compare two strings
    - B. Assign a string to `name`
    - C. Delete `name`
    - D. Print `Alice`

25. Which variable is case-sensitive in Python?
    - A. `name`
    - B. `Name`
    - C. Both are different
    - D. Neither is different

26. Which of these is an immutable data type?
    - A. `list`
    - B. `dict`
    - C. `tuple`
    - D. `set`

27. Which code correctly creates a variable with a value?
    - A. `create name = "A"`
    - B. `name = "A"`
    - C. `let name = "A"`
    - D. `name := "A"`

28. What is the output of `age = 18; print(age)`?
    - A. `18`
    - B. `age`
    - C. `Error`
    - D. `None`

29. What is the purpose of constants by convention?
    - A. To create errors
    - B. To show values should not change
    - C. To replace variables
    - D. To delete values

30. What does a variable store?
    - A. Only strings
    - B. Only numbers
    - C. A value
    - D. Only booleans

31. Why are descriptive variable names useful?
    - A. They make code harder to read
    - B. They improve readability
    - C. They speed up Python automatically
    - D. They create errors

32. Which is a correct example of multiple assignment?
    - A. `x + y = 1, 2`
    - B. `x, y = 1, 2`
    - C. `x = y = 1, 2`
    - D. `x := 1, 2`

33. What is the output of `a = 3; b = 4; print(a + b)`?
    - A. `34`
    - B. `7`
    - C. `Error`
    - D. `a + b`

34. Which of the following is not a valid variable name?
    - A. `student_name`
    - B. `studentName`
    - C. `student-name`
    - D. `studentname`

35. What does the `type()` function return?
    - A. A string value
    - B. A memory address
    - C. The data type
    - D. A boolean

36. Which statement is correct?
    - A. Variables can only store integers
    - B. Variables can store many kinds of values
    - C. Variables cannot be reassigned
    - D. Variables always need to be declared first

37. What does `x = 5` represent?
    - A. Comparison
    - B. Assignment
    - C. Function definition
    - D. Loop definition

38. Which of the following is a good example of a variable name?
    - A. `x1`
    - B. `student_age`
    - C. `a2b`
    - D. `my-variable`

39. What does `print(type("Hello"))` show?
    - A. `<class 'int'>`
    - B. `<class 'str'>`
    - C. `<class 'bool'>`
    - D. `<class 'float'>`

40. What is the purpose of `id()` in debugging?
    - A. To check the memory identity
    - B. To compare two strings
    - C. To create variables
    - D. To delete variables

41. Which of these is the best practice for naming variables?
    - A. Use very short names always
    - B. Use descriptive names
    - C. Use spaces in names
    - D. Use random symbols

42. What is the output of `x = 2; y = 3; print(x * y)`?
    - A. `5`
    - B. `6`
    - C. `23`
    - D. `Error`

43. Which of the following is not a reserved keyword?
    - A. `for`
    - B. `class`
    - C. `name`
    - D. `return`

44. What does the line `a = b = 10` do?
    - A. Creates one variable only
    - B. Assigns the same value to both variables
    - C. Swaps values
    - D. Causes an error

45. What would happen if you wrote `age = 20; age = "twenty"`?
    - A. It would fail
    - B. It would reassign the variable to a different type
    - C. It would create a new keyword
    - D. It would erase Python

46. What is the purpose of a variable in expressions?
    - A. To store and reuse values
    - B. To create syntax errors
    - C. To rename built-in functions
    - D. To stop the program

47. Which one is the correct way to compare values?
    - A. `a = b`
    - B. `a == b`
    - C. `a != b`
    - D. `a := b`

48. What does `state = "active"` mean?
    - A. Compare values
    - B. Assign a string to `state`
    - C. Delete `state`
    - D. Print `active`

49. Which is a correct statement about Python variables?
    - A. They always have fixed types
    - B. They are case-sensitive
    - C. They cannot be reassigned
    - D. They must always use uppercase

50. Which of the following statements is true?
    - A. Variables cannot hold strings
    - B. Variables can be used in calculations
    - C. Variable names can contain spaces
    - D. Variables cannot be reassigned

---

## True/False Questions (20)

1. A variable is a named container for data. (True)
2. `=` is used for comparison. (False)
3. Variable names can contain spaces. (False)
4. Python variables are case-sensitive. (True)
5. `class` can be used as a variable name. (False)
6. A variable can be reassigned. (True)
7. `type()` checks the type of an object. (True)
8. `id()` returns the value of the variable. (False)
9. `snake_case` is recommended by PEP 8. (True)
10. Variables can only hold integers. (False)
11. `name` and `Name` are the same variable. (False)
12. Variables can be used in arithmetic expressions. (True)
13. You should use reserved keywords as variable names. (False)
14. `del name` removes the variable. (True)
15. Variables always require a data type declaration. (False)
16. Python uses dynamic typing. (True)
17. Constants are usually written in uppercase. (True)
18. Descriptive names are better than short names. (True)
19. `==` is used for assignment. (False)
20. A variable can store a string. (True)

---

## Short Questions (20)

1. What is a variable?
2. What is the difference between `=` and `==`?
3. Give one example of a valid variable name.
4. Give one example of an invalid variable name.
5. What does `type()` do?
6. What does `id()` do?
7. What is dynamic typing?
8. What is PEP 8?
9. Why should variable names be descriptive?
10. What is multiple assignment?
11. What is reassignment?
12. What is the purpose of `del`?
13. Give one example of a keyword.
14. What is the difference between mutable and immutable objects?
15. What does a constant by convention mean?
16. Why is case sensitivity important?
17. How do you swap two variables in Python?
18. What happens when you assign a new value to an existing variable?
19. What is a good naming convention for variables?
20. Why are variables important in programming?

---

## Output Prediction Questions (15)

1. What is the output of:
```python
x = 5
print(x)
```

2. What is the output of:
```python
a = 3
b = 4
print(a + b)
```

3. What is the output of:
```python
name = "Ava"
print(name)
```

4. What is the output of:
```python
x, y = 2, 3
print(x)
print(y)
```

5. What is the output of:
```python
value = 10
value = 20
print(value)
```

6. What is the output of:
```python
price = 5
quantity = 2
total = price * quantity
print(total)
```

7. What is the output of:
```python
a = 8
b = 3
a, b = b, a
print(a, b)
```

8. What is the output of:
```python
age = 21
print(type(age))
```

9. What is the output of:
```python
name = "Liam"
print("Hello", name)
```

10. What is the output of:
```python
x = 5
x = x + 1
print(x)
```

11. What is the output of:
```python
x = 2
print(x * 3)
```

12. What is the output of:
```python
city = "Paris"
print(city)
```

13. What is the output of:
```python
first = 1
second = 2
print(first, second)
```

14. What is the output of:
```python
is_student = True
print(is_student)
```

15. What is the output of:
```python
score = 100
print(score)
```

---

## Debugging Questions (15)

1. Why does this code fail?
```python
2name = "A"
```

2. Why does this code fail?
```python
class = 5
```

3. What is wrong with this code?
```python
if age = 20:
    print(age)
```

4. What is wrong with this code?
```python
name == "Alice"
```

5. Find the issue:
```python
age = 20
print(ag)
```

6. Find the issue:
```python
x = 10
print(x)
```

7. What is wrong with this code?
```python
name = "Sam"
print(name)
```

8. Why might this code be considered poor style?
```python
x = 10
```

9. What is wrong with this code?
```python
a = 5
b = 8
swap(a, b)
```

10. Why does this code produce unexpected behavior?
```python
name = "Mina"
name = 25
```

11. What is wrong with this code?
```python
price = 10
quantity = 2
print(price * quantity)
```

12. What is wrong with this code?
```python
student_name = "Nora"
print(Student_Name)
```

13. Find the issue:
```python
age = 18
print(type(ag))
```

14. What is wrong with this code?
```python
x = 2
print(x + y)
```

15. Why is this code not ideal?
```python
a = b = c = 1
```

---

## Interview Questions (10)

1. What is a variable in Python?
2. What is the difference between `=` and `==`?
3. What is dynamic typing?
4. What are the rules for naming variables?
5. What is the purpose of `type()`?
6. What is the purpose of `id()`?
7. What is multiple assignment?
8. What is variable reassignment?
9. What is a keyword in Python?
10. Why is PEP 8 important?

---

## Answer Key

### Multiple Choice
1. B 2. B 3. C 4. B 5. C 6. B 7. C 8. B 9. B 10. B
11. B 12. C 13. B 14. C 15. B 16. B 17. B 18. C 19. C 20. B
21. A 22. C 23. B 24. B 25. C 26. C 27. B 28. A 29. B 30. C
31. B 32. B 33. B 34. C 35. C 36. B 37. B 38. B 39. B 40. A
41. B 42. B 43. C 44. B 45. B 46. A 47. B 48. B 49. B 50. B

### True/False
1. True 2. False 3. False 4. True 5. False 6. True 7. True 8. False 9. True 10. False
11. False 12. True 13. False 14. True 15. False 16. True 17. True 18. True 19. False 20. True

### Short Questions
Answers can be written by the learner and checked against the lesson content.

### Output Prediction
Expected outputs follow the code shown in each example.

### Debugging
Answers should explain the naming, syntax, or logic mistake in each snippet.

### Interview Questions
Answers should be based on the concepts explained in this module.
