"""
Exercise 16: Polymorphism

Problem:
Create multiple classes that provide the same method and use
their objects through a common interface.

Requirements:
1. Define a class named Dog with a speak() method.
2. The Dog speak() method should display:
   "Dog says: Woof!"
3. Define a class named Cat with a speak() method.
4. The Cat speak() method should display:
   "Cat says: Meow!"
5. Define a class named Cow with a speak() method.
6. The Cow speak() method should display:
   "Cow says: Moo!"
7. Create one object from each class.
8. Store the objects in a collection.
9. Use a loop to call speak() on each object.
10. Do not use isinstance() or explicit type checking.

Expected Behavior:
The program should display:

Dog says: Woof!
Cat says: Meow!
Cow says: Moo!
"""