# ======== Exercise 3: Valid and invalid identifiers ========

# Five valid identifiers

valid_identifier1 = 1
name = "Shehbaz"
student_age = 20
courseName = "BSAI"
_underscore_ok = True

# Five invalid identifiers
# 1. Starts with a number
# 2. Contains a hyphen      
# 3. Contains a space
# 4. Uses a reserved keyword    
# 5. Uses special characters

# invalid1 = 10  # invalid because identifier cannot start with a digit
# invalid-identifier = 20  # invalid because hyphens are not allowed
# invalid identifier = 30  # invalid because spaces are not allowed
# for = 5  # invalid because "for" is a reserved keyword
# invalid$char = 40  # invalid because "$" is not allowed
