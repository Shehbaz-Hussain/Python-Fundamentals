# Functions Assignment

## Instructions

- Complete all questions using only the concepts covered in the **Functions** module.
- Write clean, readable, and well-formatted Python code.
- Use meaningful variable and function names.
- Follow PEP 8 guidelines where applicable.
- Test your programs with different inputs before considering them complete.

---

## Question 1: Welcome Function

Write a function named `welcome()` that displays the following message:

```
Welcome to the Python Programming Foundation!
```

Call the function three times.

---

## Question 2: Personalized Greeting

Write a function named `greet_user()` that accepts a user's name as a parameter and displays a personalized greeting.

Ask the user to enter their name and call the function.

---

## Question 3: Calculator Function

Write a function named `calculate_sum()` that accepts two numbers as parameters and returns their sum.

Ask the user to enter two numbers, call the function, store the returned value, and display the result.

---

## Question 4: Even or Odd

Write a function named `check_even_odd()` that accepts one integer as a parameter.

The function should return:

- `"Even"` if the number is even.
- `"Odd"` if the number is odd.

Ask the user for a number and display the returned result.

---

## Question 5: Largest Number

Write a function named `find_largest()` that accepts three numbers as parameters and returns the largest value.

Display the returned result.

---

## Question 6: Grade Calculator

Write a function named `calculate_grade()` that accepts a student's marks as a parameter.

Return the appropriate grade using the following criteria:

| Marks | Grade |
|-------:|:-----:|
| 90–100 | A |
| 80–89 | B |
| 70–79 | C |
| 60–69 | D |
| Below 60 | F |

Ask the user for marks and display the returned grade.

---

## Question 7: Rectangle Area

Write a function named `calculate_area()` that accepts the length and width of a rectangle.

Return the calculated area.

Display the returned value.

---

## Question 8: Temperature Converter

Write a function named `celsius_to_fahrenheit()` that accepts a temperature in Celsius and returns the equivalent temperature in Fahrenheit.

Display the converted temperature.

---

## Question 9: Employee Salary

Write a function named `calculate_gross_salary()` that accepts a basic salary.

Calculate and return the gross salary using:

- House Allowance = 20% of basic salary
- Medical Allowance = 10% of basic salary

Display the gross salary.

---

## Question 10: Student Result System

Write a program that uses multiple functions to perform the following tasks:

- Accept a student's name.
- Accept marks for three subjects.
- Calculate the total marks.
- Calculate the percentage.
- Calculate the grade.
- Display a formatted result report.

Use separate functions for each task wherever appropriate.